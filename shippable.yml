language: scala
scala:
- 2.10.4
jdk:
- openjdk7
cache:
  directories:
  - $HOME/.ivy2/cache
before_install:
  - env
  - mkdir -p ~/.sbt/0.13
  - scp cred@utility.allenai.org:~/credentials.sbt ~/.sbt/0.13/
script:
  - sbt -J-Xmx8G test
env:
  global:
    - SBT_OPTS=-Xmx8G
